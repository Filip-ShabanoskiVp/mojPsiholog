@model mojPsihologApp.Models.PacientotZakazuvaTermin

@{
    ViewData["Title"] = "Create";
}

<h1>Закажување термин</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="idTermin" class="control-label"></label>
                <select asp-for="idTermin" class ="form-control" asp-items="ViewBag.idTermin">
                    <option value="">-- Избери --</option>
                </select>
                <p class="col-12 text-center text-danger">@Html.ValidationMessage("Error")</p>
            </div>
            <div class="form-group">
                <input type="submit" value="Закажи" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
